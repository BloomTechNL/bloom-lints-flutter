name: test_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  bloom_lints:
    path: ../

  # Add this line for the custom lint rules to work
  custom_lint: ^0.5.3

flutter:
  uses-material-design: true
